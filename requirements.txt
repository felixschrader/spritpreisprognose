# =============================================================================
# requirements.txt
# Python-Abhängigkeiten für das Spritpreisprognose-Projekt
#
# Installation: pip install -r requirements.txt
# =============================================================================


# -----------------------------------------------------------------------------
# Datenverarbeitung
# -----------------------------------------------------------------------------

# pandas: Kernbibliothek für Tabellen (DataFrames) — wird überall verwendet
pandas>=2.0

# numpy: Numerische Berechnungen (Grundlage für pandas und scikit-learn)
numpy>=1.24

# pytz: Zeitzonen-Unterstützung — für korrekte Berliner Zeitstempel
pytz

# python-dotenv: lädt .env-Dateien als Umgebungsvariablen
# → API-Keys sicher aus dem Code raushalten
python-dotenv>=1.0
beautifulsoup4

# -----------------------------------------------------------------------------
# Datenabruf
# -----------------------------------------------------------------------------

# requests: HTTP-Anfragen — für EZB-API (EUR/USD) und andere REST-APIs
requests>=2.31

# yfinance: Finanzdaten von Yahoo Finance — für Brent Futures (BZ=F)
# Kein API-Key nötig, kostenlos
yfinance


# -----------------------------------------------------------------------------
# Analyse & Visualisierung
# -----------------------------------------------------------------------------

# matplotlib: Standard-Bibliothek für Diagramme in Python
matplotlib>=3.7

# seaborn: Aufbauend auf matplotlib, schönere statistische Plots
seaborn>=0.12


# -----------------------------------------------------------------------------
# Jupyter Notebooks
# -----------------------------------------------------------------------------

# jupyter: Notebook-Server und -Interface
jupyter>=1.0

# ipykernel: Python-Kernel für Jupyter — verbindet Python mit dem Notebook
ipykernel>=6.0

# ipywidgets: interaktive Widgets im Notebook (z.B. Dropdown, Slider, Progress-Bars)
ipywidgets>=8.0


# -----------------------------------------------------------------------------
# Maschinenlernen (wird später aktiviert)
# -----------------------------------------------------------------------------

# scikit-learn: ML-Bibliothek — für Prognosemodelle (MVP-Phase)
# scikit-learn>=1.3


# -----------------------------------------------------------------------------
# Optional / bei Bedarf aktivieren
# -----------------------------------------------------------------------------

# pyarrow: Parquet-Unterstützung — wird im Notebook für Caching der Tankerkönig-Daten verwendet
pyarrow>=12.0

# openpyxl: Excel-Dateien lesen/schreiben
# openpyxl>=3.1

# tqdm: Fortschrittsbalken mit ETA — für tankerkoenig_pipeline.py
tqdm>=4.0